[package]
name = "estimate-core"
version = "0.1.0"
edition = "2021"
description = "Native E-Estimate canal and rate-analysis helpers"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
